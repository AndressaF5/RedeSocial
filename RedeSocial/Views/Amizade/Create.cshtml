@model Dominio.Amizade

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Amizade</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UsuarioIdB" class="control-label"></label>
                <select asp-for="UsuarioIdB" class ="form-control" asp-items="ViewBag.UsuarioIdB"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Seguir" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar para a lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
