@model Dominio.Oficina

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Oficina</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomeOficina" class="control-label"></label>
                <input asp-for="NomeOficina" class="form-control" />
                <span asp-validation-for="NomeOficina" class="text-danger"></span>
                <div class="form-group">
                    <div class="form-group">
                        <label asp-for="QtdParticipantes" class="control-label"></label>
                        <input asp-for="QtdParticipantes" class="form-control" />
                        <span asp-validation-for="QtdParticipantes" class="text-danger"></span>
                    </div>
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
